# تحديثات صفحة الطلبات - ملخص التغييرات

## ✅ تم الإنجاز:

### 1. إضافة نماذج جديدة (الأسطر 19-73 في main.dart):
- `OrderStatus`: فئة تحتوي على حالات الطلب (قيد المعالجة، تم التسعير، تمت الموافقة، قيد التجهيز، منتهي، ملغى)
- `CartItem`: فئة لعناصر سلة التسوق

### 2. الميزات التي يجب إضافتها لـ OrdersScreen:

#### أ. تبويبات حسب مرحلة الطلب (6 تبويبات):
1. قيد المعالجة (pending/submitted)
2. تم التسعير (priced)
3. تمت الموافقة (approved)
4. قيد التجهيز (preparing)
5. منتهي (shipping)
6. ملغى (cancelled)

#### ب. فلترة حسب الزبون:
- تظهر فقط للمدير والمحاسب
- زر "عرض فواتير زبون" يفتح قائمة بالزبائن
- عند اختيار زبون، تظهر طلباته فقط

#### ج. تعليم الطلبات الجديدة:
- نقطة خضراء + علامة "جديد" للطلبات غير المقروءة
- يتم تعليم الطلب كمقروء عند فتحه (API: markOrderAsRead)

#### د. معلومات إضافية في كل طلب:
- رقم الطلب التسلسلي
- اسم الزبون
- التاريخ والوقت
- حالة الطلب مع لون مميز

#### هـ. ترتيب الطلبات:
- الأحدث أولاً حسب `created_at` أو `date`

## 📝 الخطوات التالية المطلوبة في Backend:

1. **إضافة حقول جديدة في جدول الطلبات:**
   - `is_read` (boolean) - هل تم قراءة الطلب
   - `is_new` (boolean) - هل هو طلب جديد
   
2. **إنشاء endpoint جديد:**
   ```
   POST markOrderAsRead
   Parameters: orderId, username, token
   ```

3. **تحديث endpoint getOrders:**
   - دعم الفلترة بـ `customer_id`
   - إرجاع الحقول الجديدة

4. **تحديث حالات الطلب في قاعدة البيانات:**
   - pending, priced, approved, preparing, shipping, cancelled

## 🔧 التطبيق الحالي:

النماذج موجودة في: `/workspace/mobile_app/lib/main.dart`
- OrderStatus: السطر 19
- CartItem: السطر 58

لتحديث OrdersScreen بالكامل، انسخ الكود من `/workspace/orders_page_update.md` واستبدل class `_OrdersScreenState` بالكامل.
